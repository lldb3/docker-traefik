http:
  routers:
    restic-rtr:
      rule: "Host(`restic.meln.ovh`)" 
      entryPoints:
        - websecure
      middlewares:
        - chain-no-auth
      service: restic-svc
  services:
    restic-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.3.10:8000"
